# NarrativeLab 1.2.2

A small Navigator follow-up to 1.2.1.

## Fixes

- Series titles that do not contain the currently open project are now muted, matching inactive books. Only the series that holds the active project stays at full contrast.

## Compatibility

Requires Obsidian 1.12.7 or later. Installation is unchanged: `main.js`, `manifest.json`, and `styles.css`.
